extends Node2D
class_name MapManager

# @onready var parallax_bg: Sprite2D = $Background

# func _ready() -> void:
# 	GameManager.map_manager = self

# func _process(_delta: float) -> void:
# 	parallax_bg.global_position.x = GameManager.camera.global_position.x
# 	parallax_bg.global_position.clamp(GameManager.camera.min_coord, GameManager.camera.max_coord)
